version: '3.1'
services:
  stuff-service:
    build: ./stuff
    ports:
      - 80:80
    environment:
      - TEST_VAL
      - SQUASH_BRANCH_WITH_ID
      - SQUASH_DOMAIN

  stuff-service2:
    build:
      context: ./stuff
      dockerfile: DockerfileA
    ports:
      - 81:80
    environment:
      - TEST_VAL
      - SQUASH_BRANCH_WITH_ID
      - SQUASH_DOMAIN

  stuff-service3:
    build:
      context: ./stuff
      dockerfile: DockerfileB
    ports:
      - 8000:80
    environment:
      - TEST_VAL
      - SQUASH_BRANCH_WITH_ID
      - SQUASH_DOMAIN
